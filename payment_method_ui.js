(()=>{
const labels={cash:'Cash',bank:'Online / bank'};
function installField(){const form=document.getElementById('transactionForm');if(!form||document.getElementById('paymentMethodLabel'))return;const status=document.getElementById('statusLabel');const label=document.createElement('label');label.id='paymentMethodLabel';label.className='transaction-fields';label.innerHTML='<span>Betaalmethode</span><select id="paymentMethod"><option value="bank">Online / bank</option><option value="cash">Cash</option></select>';status?.insertAdjacentElement('afterend',label)}
function syncVisibility(){const type=document.getElementById('transactionType')?.value;const label=document.getElementById('paymentMethodLabel');if(label)label.hidden=type==='item'}
function renderMethods(){document.querySelectorAll('#incomingCards .transaction-card,#outgoingCards .transaction-card').forEach(card=>{const edit=card.querySelector('[data-edit-transaction]');if(!edit||card.querySelector('.payment-method-line'))return;const tx=state.transactions.find(t=>t.id===edit.dataset.editTransaction);if(!tx)return;const paid=tx.type==='incoming'?!!tx.paid:!!tx.done;if(!paid)return;const controls=card.querySelector('.transaction-controls');if(!controls)return;const line=document.createElement('div');line.className='payment-method-line';line.innerHTML=`<small>Betaalmethode</small><strong>${labels[tx.paymentMethod]||'Niet opgegeven'}</strong>`;controls.insertAdjacentElement('beforebegin',line)})}
function rerender(){if(typeof render==='function')render();requestAnimationFrame(renderMethods)}
window.addEventListener('load',()=>{installField();syncVisibility();setTimeout(renderMethods,300)});
document.addEventListener('click',e=>{const typeBtn=e.target.closest('.type-switch [data-type]');if(typeBtn)setTimeout(syncVisibility,0);const edit=e.target.closest('[data-edit-transaction]');if(edit)setTimeout(()=>{installField();const tx=state.transactions.find(t=>t.id===edit.dataset.editTransaction);const select=document.getElementById('paymentMethod');if(select)select.value=tx?.paymentMethod||'bank';syncVisibility()},0);if(e.target.closest('[data-add],#quickAddBtn'))setTimeout(()=>{installField();const select=document.getElementById('paymentMethod');if(select)select.value='bank';syncVisibility()},0);if(e.target.closest('[data-toggle],[data-toggle-paid]'))setTimeout(renderMethods,80)});
document.addEventListener('submit',e=>{if(e.target.id!=='transactionForm')return;const type=document.getElementById('transactionType')?.value;if(type==='item')return;const method=document.getElementById('paymentMethod')?.value||'bank';const editId=typeof editingTransactionId!=='undefined'?editingTransactionId:null;setTimeout(()=>{let tx=editId?state.transactions.find(t=>t.id===editId):null;if(!tx){tx=[...state.transactions].filter(t=>t.type===type).sort((a,b)=>new Date(b.date)-new Date(a.date))[0]}if(!tx)return;tx.paymentMethod=method;if(typeof save==='function')save();rerender()},0)},true);
const observer=new MutationObserver(records=>{for(const r of records){for(const n of r.addedNodes){if(n.nodeType===1&&(n.matches?.('.transaction-card')||n.querySelector?.('.transaction-card'))){requestAnimationFrame(renderMethods);return}}}});['incomingCards','outgoingCards'].forEach(id=>{const el=document.getElementById(id);if(el)observer.observe(el,{childList:true,subtree:true})});
})();
